participants:
  - el_type: geth
    cl_type: lighthouse
    count: 2
    # cl_extra_params: [--checkpoint-sync-url="http://188.245.49.86:32826"]

# Default configuration parameters for the network
network_params:
  # Network name, used to enable syncing of alternative networks
  # Defaults to "kurtosis"
  # You can sync any public network by setting this to the network name (e.g. "mainnet", "sepolia", "holesky")
  # You can sync any devnet by setting this to the network name (e.g. "dencun-devnet-12", "verkle-gen-devnet-2")
  network: "kurtosis"

  # The network ID of the network.
  network_id: "12020498"

additional_services:
  # - assertoor
  # - broadcaster
  # - tx_spammer
  # - blob_spammer
  # - custom_flood
  # - goomy_blob
  # - el_forkmon
  - blockscout
  # - beacon_metrics_gazer
  # - dora
